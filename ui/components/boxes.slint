
import { Theme } from "../theme.slint";
export component hbox inherits HorizontalLayout {
    padding: Theme.spacing-md;
    alignment: start;
}

export component vbox inherits VerticalLayout {
    padding: Theme.spacing-md;
    alignment: start;
}